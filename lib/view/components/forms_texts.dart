import 'package:flutter/material.dart';

Widget custom_text(String x) {
  return Text(x,
      style: TextStyle(
        color: Colors.black,
        fontSize: 30.0,
        fontWeight: FontWeight.bold,
      ));
}
